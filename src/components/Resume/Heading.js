const Heading = ({ title }) => {
  return (
    <div className="mb-4 text-xl font-bold text-slate-100 underline decoration-orange-600 decoration-2 underline-offset-8">
      {title}
    </div>
  );
};

export default Heading;
